import React from "react";

export default () => <span>Sub</span>;

// Hack to
// import _styled from "styled-components";

// const styled: typeof _styled = (_styled as any).default || _styled;

// export default () => <Red>Sub Styled</Red>;

// const Red = styled.span`
//   color: red;
// `;
